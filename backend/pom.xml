<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>ro.garajulmeu</groupId>
	<artifactId>backend</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>backend</name>
	<description>Garajul Meu backend API</description>

	<properties>
		<java.version>21</java.version>
		<maven.compiler.proc>full</maven.compiler.proc>

		<!--
			Pinned, because spring-boot-starter-parent does not manage this
			plugin - checked, not assumed. Bump freely; nothing here depends on
			the exact version.
		-->
		<jacoco.version>0.8.13</jacoco.version>

		<!--
			Also unmanaged by the parent, and the artifact name is version-
			specific: Spring Boot 4 takes `sentry-spring-boot-4`, where 2 and 3
			took `sentry-spring-boot-starter` and `-starter-jakarta`. Copying a
			snippet from the wrong tab of Sentry's own documentation is the easy
			mistake here, and it fails as an unresolvable artifact rather than
			as anything subtle.
		-->
		<sentry.version>8.53.0</sentry.version>

		<!--
			Declared empty on purpose. Surefire's argLine below references
			@{jacocoArgLine}, and if JaCoCo's prepare-agent has not run - a
			build with -Djacoco.skip=true, or a goal that skips initialize -
			the reference would otherwise survive as the literal text
			"@{jacocoArgLine}" and be handed to the JVM, which fails to start
			with an unreadable error. An empty default makes that case a no-op.
		-->
		<jacocoArgLine></jacocoArgLine>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcprov-jdk18on</artifactId>
			<version>1.85.2</version>
		</dependency>
				<!--
			Document AI, pinned rather than managed: this project has no Google BOM
			and needs exactly one of their libraries. Section 32's provider seam is
			what keeps this dependency from reaching further than the adapter.
		-->
		<dependency>
			<groupId>com.google.cloud</groupId>
			<artifactId>google-cloud-document-ai</artifactId>
			<version>2.97.0</version>
		</dependency>

		<!--
			Section 27: "Sentry for unexpected exceptions; expected 4xx business
			errors are not treated as crashes."

			`sentry-logback` is declared explicitly rather than left to arrive
			transitively, because it is the mechanism the whole design rests on.
			GlobalExceptionHandler has logged business failures at INFO and
			unhandled ones at ERROR since phase 3, and its javadoc has said since
			then that this is what Sentry would pick up. The Logback appender
			turns ERROR logs into events, so that division of log levels *is* the
			filter section 27 asks for - and it also catches errors thrown in
			filters and in the reminder scheduler, which never reach a
			@ControllerAdvice at all. A dependency that load-bearing should be
			written down, not hoped for.
		-->
		<dependency>
			<groupId>io.sentry</groupId>
			<artifactId>sentry-spring-boot-4</artifactId>
			<version>${sentry.version}</version>
		</dependency>
		<dependency>
			<groupId>io.sentry</groupId>
			<artifactId>sentry-logback</artifactId>
			<version>${sentry.version}</version>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-resource-server-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>properties</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!--
						Two agents, one argLine, and only one of them can be
						named by the default arrangement.

						JaCoCo's prepare-agent writes its -javaagent flag into a
						property called `argLine` unless told otherwise, and
						this element - being explicit configuration - wins over
						that property outright. The two ways to get this wrong
						are both silent: leave it as it was and coverage reports
						0% everywhere while every test passes, or let JaCoCo have
						`argLine` and Mockito's agent disappears instead.

						So JaCoCo is redirected to `jacocoArgLine` (see its
						plugin configuration) and both are named here.

						@{...}, not ${...}, and the difference is the whole
						trick. ${...} is interpolated when the POM is read,
						before any plugin has run, so it would resolve to the
						empty default above and the agent would never attach.
						@{...} is Surefire's late replacement, evaluated when
						Surefire itself executes - by which time prepare-agent
						has run in the initialize phase and set the property.
					-->
					<argLine>@{jacocoArgLine} -javaagent:${org.mockito:mockito-core:jar}</argLine>
				</configuration>
			</plugin>
			<!--
				Coverage as a map, not as a gate. There is deliberately no
				`check` goal and no minimum: a threshold rewards tests written to
				move a number, and this project's real guards - the ones that
				assert in both directions, like `errorKey.test.ts` or `CorsTest`
				reading Spring's own route table - already do more than a
				percentage can. What the report is for is finding code no test
				has ever executed.

				Nothing is excluded, for the same reason. Hiding parts of the map
				is how a map starts lying.
			-->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<propertyName>jacocoArgLine</propertyName>
						</configuration>
					</execution>
					<execution>
						<!-- Default phase is verify, which is where we run it. -->
						<id>report</id>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>